import { TicketDetailsModel } from "./ticket-details-model";

describe("TicketDetailsModel", () => {
  it("should create an instance", () => {
    expect(new TicketDetailsModel()).toBeTruthy();
  });
});
